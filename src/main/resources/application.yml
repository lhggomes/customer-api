spring:
  datasource:
    driverClassName: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3219/customer_application?rewriteBatchedStatements=true&useSSL=false&autoReconnect=true
    username: admin
    password: acB17IGHaX
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect